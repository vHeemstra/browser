import 'cypress';

// import { BrowserBarcodeReader } from '../../src/BrowserBarcodeReader';

describe('My First Test', () => {
    it('Does not do much!', () => {
        // const test = new BrowserBarcodeReader();
        expect(true).to.equal(true);
    });
});
